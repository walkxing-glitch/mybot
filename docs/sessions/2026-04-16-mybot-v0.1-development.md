# 2026-04-16 — MyBot v0.1 开发会话记录

> 从 0 到 1：一天内完成 MyBot agent 的设计、实施、上线。

---

## 时间线

### 1. 需求澄清（brainstorming）

**起点：** 用户提出"想开发一个类似 Nous Research Hermes Agent 的个人 AI agent"。

**通过 brainstorming skill 逐步澄清：**

| 维度 | 用户选择 | 原因 |
|------|---------|------|
| 使用场景 | A + B + C 全部 | 个人助手 / 开发工具 / Neural-Twin 前端 |
| LLM 选型 | D) 多模型支持 | Claude / OpenAI / 豆包 / Ollama 一行切换 |
| 技术栈 | A) 纯 Python | 与 Neural-Twin / myontology 同栈 |
| 记忆系统 | D + E + F | 画像演化 + 遗忘衰减 + 跨会话时间推理 |
| 交付时间 | 今天要能跑 | 先搭骨架跑通，迭代加功能 |
| 容器化 | 单独 Docker 容器 | 通过 external network 连接 myontology |
| 本体论角色 | 作为 agent 大脑 | HTTP 调用 myontology API，不复制数据 |

### 2. 方案决策

**三方案对比后选择方案二（LiteLLM + 插件化工具 + 高级记忆）：**

- 方案一（Hermes 完全复刻）：工作量巨大，大部分功能短期用不到，**否**
- 方案二：LiteLLM 统一多模型 + 插件工具 + 自研记忆引擎，**选**
- 方案三（LangGraph）：依赖重，抽象层多，**否**

### 3. 设计规格

**写入：** `docs/superpowers/specs/2026-04-16-mybot-agent-design.md`（389 行）

**核心架构：**

```
Gateway (CLI + Telegram)
    ↓
Agent Core (LiteLLM + Tool Router + Conversation Manager)
    ↓
┌─────────────────┬──────────────────┐
│   Tools (8个)   │  Memory Engine   │
│  shell/code/    │  Profile演化     │
│  web/ontology/  │  Decay衰减       │
│  neural_twin/   │  Temporal推理    │
│  calendar/memory│  SQLite+FTS5     │
└─────────────────┴──────────────────┘
```

**记忆引擎三大特性（核心差异化）：**

1. **用户画像演化** — 5 个维度（behavior/interest/decision_style/social/focus），每次对话 LLM 输出 diff（update/strengthen/weaken/new_insight/trend_change）
2. **遗忘衰减** — salience = base_importance × e^(-λt) × access_boost × relevance；按类型不同半衰期（episode 30d / fact 180d / preference 365d）
3. **跨会话时间推理** — 记忆带时间戳和 temporal_context，检索时注入时间上下文让 LLM 感知"什么过时了"

### 4. 并行实施

**派 4 个 agent 并行开发**，每个负责一个独立域：

| Agent | 负责模块 | 输出 |
|-------|---------|------|
| 1. Scaffolding | pyproject / Dockerfile / docker-compose / config / __main__ / llm / utils / tools base | 14 个文件 |
| 2. Memory Engine | store / decay / profile / temporal / engine | 5 个文件 |
| 3. Tools | shell / code / web / ontology / neural_twin / calendar / memory_tool | 7 个文件 |
| 4. Agent Core + Gateway | agent / gateway/cli / gateway/telegram | 3 个文件 |

**意外情况：** agent 没在独立 worktree 里跑，互相踩踏对方的文件。但每个 agent 最后正确 stash + 切分支 + 提交，合并时零冲突。

**分支结构：**
```
main
 └─ feat/scaffolding
     └─ feat/memory-engine
         └─ feat/agent-core-gateways ← 合并 feat/agent-tools
main ← merge feat/agent-core-gateways
```

### 5. 集成修复

**问题：** CLI 调用 `load_enabled_tools()` 但该函数不存在；MemoryTool 需要 memory_engine 注入，但 auto_discover 试图无参实例化会失败。

**修复：**
- 在 `mybot/tools/__init__.py` 添加 `load_enabled_tools(config, memory_engine=None)` 函数
- auto_discover 跳过 `memory_tool` 模块（列入 SKIP_MODULES）
- 调整 cli.py 和 telegram.py 的 bootstrap 顺序：先建 memory_engine，再加载 tools

### 6. 验证

**烟雾测试全部通过：**
- ✅ 8 个工具加载成功
- ✅ 记忆引擎 insert + FTS 搜索 OK
- ✅ 衰减计算正确（episode 30 天后 salience=0.305）
- ✅ Shell 工具执行 `echo hello` 返回正常
- ✅ Calendar 工具 list 操作 OK
- ✅ Agent system prompt 生成 1923 字符

**真实 LLM 调用（DeepSeek API）：**
- ✅ 纯对话：DeepSeek 返回中文自我介绍
- ✅ 工具调用：agent 识别 shell 意图，调用成功，解读结果

### 7. 上线

**Telegram Bot：**
- BotFather 创建：`@xingzhiqiang_claude_fst_bot`（ID: 8699209019）
- Token 写入 `.env`（gitignore 保护）
- 长轮询模式启动成功

**持久化（launchd）：**
- 部署：`~/Library/LaunchAgents/com.xingzq.mybot.plist`
- 配置：RunAtLoad + KeepAlive + ThrottleInterval=30
- 日志：`data/mybot.log` + `data/mybot.err.log`
- 验证：PID 54716 正常运行

### 8. Neural-Twin FastAPI 包装（并行任务）

**派 1 个 agent 做 Neural-Twin 的 HTTP 封装：**

在 `/Users/ddn/Developer/02_AI_Assistants/Claude_Work/neural-twin/` 新增：
- `api_server.py` — 6 个端点（/predict, /habits/analyze, /anomaly/detect, /forecast/today, /, /health）
- `requirements.txt` 追加 fastapi/uvicorn/pydantic
- `run_api.sh` 启动脚本

**已知限制：**
- Neural-Twin 数据只到 2025 年，今天调 `/forecast/today` 返回 `insufficient_data`
- `/habits/analyze` 不传 week_start 会用 CSV 最后一周（2025 年）

---

## 最终产物

### 代码量
- ~3400 行 Python 代码
- 24 个 Python 文件
- 8 次 commit，包含 2 次合并

### 目录结构
```
mybot/
├── docker-compose.yml            # 容器编排
├── Dockerfile
├── config.yaml                   # 主配置（DeepSeek 默认模型）
├── .env                          # API keys（gitignore 保护）
├── pyproject.toml
├── mybot/
│   ├── __main__.py               # python -m mybot {cli|telegram}
│   ├── agent.py                  # 524 行 Agent 主循环
│   ├── llm.py                    # LiteLLM 多模型封装
│   ├── config.py                 # YAML + .env 加载
│   ├── utils.py
│   ├── tools/
│   │   ├── base.py               # BaseTool / ToolResult
│   │   ├── shell.py
│   │   ├── code.py
│   │   ├── web.py                # WebSearchTool + WebFetchTool
│   │   ├── ontology.py           # HTTP → myontology API
│   │   ├── neural_twin.py        # HTTP → Neural-Twin API
│   │   ├── calendar.py           # SQLite 本地日程
│   │   └── memory_tool.py        # 记忆操作暴露给 LLM
│   ├── memory/
│   │   ├── store.py              # SQLite + FTS5 持久化
│   │   ├── decay.py              # 衰减计算
│   │   ├── profile.py            # 画像演化
│   │   ├── temporal.py           # 时间推理
│   │   └── engine.py             # 核心协调器
│   └── gateway/
│       ├── cli.py                # REPL with rich
│       └── telegram.py           # python-telegram-bot v20+
├── data/                         # SQLite DB + 日志
└── docs/
    ├── superpowers/specs/
    │   └── 2026-04-16-mybot-agent-design.md
    └── sessions/
        └── 2026-04-16-mybot-v0.1-development.md  ← 本文件
```

### 运行状态
- ✅ Telegram bot `@xingzhiqiang_claude_fst_bot` 通过 launchd 常驻
- ✅ DeepSeek 作为默认 LLM
- ✅ 记忆引擎持续记录到 `data/memory.db`
- ⏸ Neural-Twin API 已开发但未启动（下一步任务）

---

## 关键设计决策

| 决策 | 原因 |
|------|------|
| LiteLLM 统一多模型 | 避免为每个 provider 写 adapter |
| 插件化工具注册 | `tools = [...]` 导出 + auto_discover |
| SQLite + FTS5 | 轻量，单文件，容器友好，FTS5 做全文搜索 |
| 记忆按类型分半衰期 | 事实和偏好衰减慢，事件和观察衰减快 |
| 本体论通过 HTTP 调用 | 不复制数据，不耦合，项目独立 |
| launchd 而非 Docker | 纯个人 daemon，Docker Desktop 开机不自启 |
| DI 而非自动注入 MemoryTool | 工具需要 engine 实例，统一在 load_enabled_tools 处理 |

---

## 项目边界（重要）

用户明确强调三个项目各自独立：

- **mybot**（本项目）— Agent 框架，通过 HTTP 调用其他项目
- **neural-twin** — 神经网络数字分身，独立训练、独立部署
- **myontology** — 实体知识图谱，独立开发中

mybot 只是前端整合层，**不混合代码**，各项目有自己的生命周期。

---

## 未完成事项（供下次恢复）

1. **Neural-Twin API 启动 + 端到端测试** — 需要验证 `/predict` 决策预测对真实场景有多大价值
2. **Neural-Twin 2026 数据补充** — 当前数据只到 2025，`/forecast/today` 和 `/habits/analyze` 不能用最新数据
3. **Neural-Twin API 可能也做成 launchd 常驻**（待用户决定）
4. **Myontology API 启动** — 如果想让 `ontology` 工具真的能用，需要 myontology 的 docker-compose 运行
5. **单元测试** — 目前只有烟雾测试，没有正式 pytest 测试
6. **用户画像演化验证** — 需要积累足够对话才能看到画像真正演化

---

## 关键命令速查

```bash
# Bot 管理
launchctl list | grep mybot                                              # 状态
tail -f /Users/ddn/Developer/02_AI_Assistants/Claude_Work/mybot/data/mybot.log   # 日志
launchctl unload ~/Library/LaunchAgents/com.xingzq.mybot.plist          # 停
launchctl load ~/Library/LaunchAgents/com.xingzq.mybot.plist            # 起

# 本地 CLI 测试
cd /Users/ddn/Developer/02_AI_Assistants/Claude_Work/mybot
.venv/bin/python -m mybot cli

# Neural-Twin API 启动
cd /Users/ddn/Developer/02_AI_Assistants/Claude_Work/neural-twin
./run_api.sh     # 监听 0.0.0.0:8004
```

---

## 下次会话恢复提示词

> 继续 MyBot 项目。当前状态：v0.1 已上线，Telegram bot @xingzhiqiang_claude_fst_bot 通过 launchd 常驻运行，DeepSeek 作为 LLM，8 个工具全部就绪。Neural-Twin FastAPI 包装已完成但未启动测试。下一步：启动 Neural-Twin API，验证 `/predict` 决策预测对真实场景（如"该不该买 X"）的价值，判断是否值得进一步开发。参考 `docs/sessions/2026-04-16-mybot-v0.1-development.md`。
